<h3> List of Students </h3>

 <% @students.each do |student| %>
<p><%= student["firstName"] + " " + student["lastName"]  %> </p>
<p> <a href ="/resume/<%= student["id"] %>"> <%=student["email"] %> </a> </p>
<hr>
<% end %> 